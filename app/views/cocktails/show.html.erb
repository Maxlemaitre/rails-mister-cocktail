<h1><%= @cocktail.name %></h1>
<ul><% @cocktail.doses.each do |dose| %>
  <li> <%= dose.description %> : <%= dose.ingredient.name %> -
  <%= link_to "Delete", dose_path(dose), method: :delete, class:"btn btn-danger" %> </li>
<% end %> </ul>
<%= link_to "Back", cocktails_path, class:"btn btn-primary" %> <%= link_to "Create a dose", new_cocktail_dose_path(@cocktail), class:"btn btn-success" %>

<!-- , data: { confirm: "Are you sure?" } -->
