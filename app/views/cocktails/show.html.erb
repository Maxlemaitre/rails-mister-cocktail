<h1><%= @cocktail.name %></h1>
<ul><% @cocktail.doses.each do |dose| %>
  <li> <%= dose.description %> : <%= dose.ingredient.name %> -
  <%= link_to "Delete", dose_path(dose), method: :delete %> </li>
<% end %> </ul>
<%= link_to "Back", cocktails_path %> <%= link_to "Create a dose", new_cocktail_dose_path(@cocktail) %>

<!-- , data: { confirm: "Are you sure?" } -->
